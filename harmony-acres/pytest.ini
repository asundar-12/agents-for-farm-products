[pytest]
# auto mode: plain `async def test_*` functions are collected without a
# per-test @pytest.mark.asyncio decorator.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts = -ra
