__pycache__/
*.pyc
.venv/
.env
.DS_Store

# Generated by `agentcore launch`/`configure` — contains deploy env vars
# (DATABASE_URL, JWT_SECRET) in plaintext, so it must never be committed.
.bedrock_agentcore.yaml
.bedrock_agentcore/

# Generated per-module docstring dumps (see extract_docstrings.py) — derived
# from source, not something to track in git.
*_docstrings.txt

# Assistant reasoning log — session notes, not source.
logs/
